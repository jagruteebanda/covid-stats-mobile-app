// import 'react-native';
import React from 'react';
import {render} from 'react-native-testing-library';
import Splash from '../app/components/Splash';

describe('Splash', () => {
  it('renders the correct app title', () => {
    // const {queryByText} = render(<Splash />);
    expect(true).toBeTruthy();
  });
});
